Take the Release folder of this build (located in the bin folder) and install on the root of C:\
Then rename the Release folder Error_Reader.
Otherwise will get errors while looking for text files that it reads from.
Can delete the app.publish foler